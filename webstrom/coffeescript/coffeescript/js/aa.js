// Generated by CoffeeScript 1.10.0
(function() {
  var changenumber, cube, data, fill, gcd, i, inner, j, kid, list, mood, out, race, singer, song, squire,
    slice = [].slice;

  for (i = j = 0; j <= 5; i = ++j) {
    console.log("hello " + i);
  }

  fill = function(container, liquid) {
    if (liquid == null) {
      liquid = "coffee";
    }
  };

  "Filling the " + container + " with " + liquid + "...";

  gcd = function(a, b) {
    if (b === 0) {
      return a;
    } else {
      return gcd(b, a % b);
    }
  };

  $("#button").click(function() {
    var a, b;
    a = $("#a").val();
    b = $("#b").val();
    return $("#c").html(gcd(a, b));
  });

  list = [1, 2, 3, 4, 5, 6];

  race = function() {
    var run, win;
    win = arguments[0], run = 2 <= arguments.length ? slice.call(arguments, 1) : [];
    return print(win, run);
  };

  squire = function(x) {
    return x * x;
  };

  cube = function(x) {
    return auire(x) * x;
  };

  fill = function(cantain, liuquid) {
    if (liuquid == null) {
      liuquid = aa;
    }
    return "fiil the " + cantain + " width " + liuquid;
  };

  song = ['a', 'bv', 'a', 'd', 'a'];

  singer = {
    a: 'b',
    c: 'd'
  };

  kid = {
    age: {
      a: 'b'
    },
    name: {
      c: 'd'
    }
  };

  $('.aa').attr({
    "class": 'bb'
  });

  console.log(object["class"]);

  out = 1;

  changenumber = function() {
    var innner;
    innner = 1;
    return out = 0;
  };

  inner = changenumber();

  if (spring) {
    mood = great;
  }

  data = a ? b : c;

}).call(this);

//# sourceMappingURL=aa.js.map
